export enum BookingRepeat {
  NOT_REPEAT = 'not_repeat',
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly'
}

export enum BookingStatus {
  PENDING = 'pending',
  APPROVE = 'approve',
  REJECT = 'reject',
}
